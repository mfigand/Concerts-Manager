<strong><h2 class="title is-2">10 Most Commented Concerts</h2></strong>
<br>
<div class="columns">
  <div class="column">
    <ul>
      <% @top_commented.each do |concert| %>
         <li><%= "#{concert[0]}"%>
           <%= link_to("Details", concert_path(concert)) %>
        </li>
      <% end%>
    </ul>
    <br>
    <%= link_to("Today Concerts", concerts_path) %>
  </div>
</div>
